/**
 * @param {character[][]} grid
 * @return {number}
 */
function numIslands(grid) {
    // Edge Case : 0
    if (!grid || grid.length === 0) return 0;

    const m = grid.length; // rows count
    const n = grid[0].length; // columns count
    let numIslands = 0;

    //breadth-first search : the idea is take the island and turn it into water


    // loop on the grid
        

    return numIslands;
}